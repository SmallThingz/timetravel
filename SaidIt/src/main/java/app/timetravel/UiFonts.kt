package app.timetravel

import android.content.Context
import android.view.View

internal object UiFonts {
    fun styleMainScreen(root: View, context: Context) = Unit

    fun styleSettings(root: View, context: Context) = Unit

    fun styleDialog(
        root: View,
        context: Context,
        titleShadowColorId: Int,
    ) = Unit
}
